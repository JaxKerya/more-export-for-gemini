# ── Dependencies ─────────────────────────────────────────────
node_modules/

# ── Build / package output ──────────────────────────────────
store/*.zip

# ── Test & validation scratch (regenerated on demand) ───────
# Everything under validate/ is generated output (debug matrix exports,
# rendered PDFs/HTML, ad-hoc re-validation files) — never commit it.
validate/
Manuel-Validate/

# referance/ holds local captures & sample exports; only the three real-DOM
# fixtures that test/extractor.mjs and scripts/export-from-dom.mjs read
# stay tracked.
referance/*
!referance/outer-html.md
!referance/sources outer-html.md
!referance/testprompt-outerhtml.md

# ── Tooling caches ───────────────────────────────────────────
.eslintcache
*.tsbuildinfo
npm-debug.log*

# ── LaTeX / external validator artifacts ─────────────────────
*.aux
*.log
*.out
*.fdb_latexmk
*.fls
*.synctex.gz

# ── OS clutter ───────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Editor / IDE / AI-agent config (local only) ─────────────
.cursor/
.claude/
agent-skills/
.vscode/
.idea/
*.swp
*.swo
*~

# ── Secrets (never commit) ───────────────────────────────────
.env
.env.*
*.pem
credentials.json
